name: GitHub Actions
run-name: ${{ github.actor }} mew mew
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
      - name: Set up python
        id: setup-python
        uses: actions/setup-python@v4
        with:
          python-version: 3.8
      - name: Install dependencies
        run: |
          pip install poetry
          poetry install
      - name: Run Linter
        run: make lint
      - name: Run tests
        run: make pytest
      - uses: paambaati/codeclimate-action@v2.2.4
      env:
        CC_TEST_REPORTER_ID: 1aa423ce88ec53b836ba7ce469e56720a2e148cc4d7d66607664cddb9b3ae6d3
      with:
        coverageCommand: poetry run pytest --cov
      - run: echo "🍏 This job's status is ${{ job.status }}."
