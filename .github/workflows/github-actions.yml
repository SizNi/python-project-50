name: GitHub Actions
run-name: ${{ github.actor }} mew mew
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code  # смотрю код репозитория
        uses: actions/checkout@v3
      - name: Python install  # установка пйтина
        uses: actions/setup-python@v3
        with: 
          python-version: "3.10"
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Poetry install  # установить поетри и зависимости ниже (поискать готовый файл)
        run: make install


      - name: Run Linter
        run: make lint
      - run: echo "🍏 This job's status is ${{ job.status }}."
